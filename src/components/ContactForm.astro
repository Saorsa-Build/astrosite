---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Contact - Saorsa Media"
  description="Get in touch with Saorsa Media. We offer premium website design, content creation, and video production services in Windsor, Ontario."
>
<section class="relative min-h-screen py-20 md:py-28 overflow-hidden bg-[var(--background)]">

  <!-- Background orbs -->
  <div class="pointer-events-none absolute inset-0 overflow-hidden">
    <div class="absolute -top-40 -right-40 w-[600px] h-[600px] rounded-full opacity-[0.06]"
      style="background: radial-gradient(circle, var(--primary), transparent 70%);">
    </div>
    <div class="absolute -bottom-40 -left-40 w-[500px] h-[500px] rounded-full opacity-[0.04]"
      style="background: radial-gradient(circle, var(--secondary), transparent 70%);">
    </div>
  </div>

  <div class="relative container mx-auto px-4 max-w-[860px]">

    <!-- Header -->
    <div class="mb-10">
      <p class="text-xs font-mono tracking-[0.2em] uppercase text-[var(--muted-foreground)] mb-4">
        Get in touch
      </p>
      <h1 class="text-5xl md:text-6xl font-bold text-[var(--foreground)] leading-[1.05] mb-5">
        Let's build<br/>
        <span style="background: linear-gradient(135deg, var(--primary), var(--secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
          something great.
        </span>
      </h1>
      <p class="text-[var(--muted-foreground)] text-lg max-w-[480px] leading-relaxed">
        Select the services you're interested in and we'll get back to you within 24 hours.
      </p>
    </div>

    <!-- Card -->
    <div class="rounded-2xl p-8 md:p-12 shadow-lg bg-[var(--card)] border border-[var(--border)]">
      <form
        action="https://formspree.io/f/mojlondj"
        method="POST"
        class="space-y-12"
      >

        <!-- Contact Information -->
        <div>
          <div class="flex items-center gap-3 mb-6">
            <span class="text-xs font-mono tracking-[0.15em] uppercase text-[var(--muted-foreground)]">01</span>
            <div class="h-px flex-1 bg-[var(--border)]"></div>
            <span class="text-sm font-semibold text-[var(--foreground)]">Contact Information</span>
          </div>

          <div class="grid md:grid-cols-2 gap-4 mb-4">
            <div>
              <label for="name" class="block text-xs font-mono tracking-wider uppercase text-[var(--muted-foreground)] mb-2">
                Name <span class="text-[var(--primary)]">*</span>
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                placeholder="John Doe"
                class="w-full px-4 py-3 rounded-xl border border-[var(--border)] bg-[var(--input)] text-[var(--foreground)] placeholder:text-[var(--muted-foreground)]/40 focus:outline-none focus:border-[var(--ring)] focus:ring-2 focus:ring-[var(--ring)]/30 transition-all text-sm"
              />
            </div>
            <div>
              <label for="phone" class="block text-xs font-mono tracking-wider uppercase text-[var(--muted-foreground)] mb-2">
                Phone
              </label>
              <input
                type="tel"
                id="phone"
                name="phone"
                placeholder="+1 (548) 000-0000"
                class="w-full px-4 py-3 rounded-xl border border-[var(--border)] bg-[var(--input)] text-[var(--foreground)] placeholder:text-[var(--muted-foreground)]/40 focus:outline-none focus:border-[var(--ring)] focus:ring-2 focus:ring-[var(--ring)]/30 transition-all text-sm"
              />
            </div>
          </div>

          <div>
            <label for="email" class="block text-xs font-mono tracking-wider uppercase text-[var(--muted-foreground)] mb-2">
              Email <span class="text-[var(--primary)]">*</span>
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              placeholder="john@example.com"
              class="w-full px-4 py-3 rounded-xl border border-[var(--border)] bg-[var(--input)] text-[var(--foreground)] placeholder:text-[var(--muted-foreground)]/40 focus:outline-none focus:border-[var(--ring)] focus:ring-2 focus:ring-[var(--ring)]/30 transition-all text-sm"
            />
          </div>
        </div>

        <!-- Website Services -->
        <div>
          <div class="flex items-center gap-3 mb-6">
            <span class="text-xs font-mono tracking-[0.15em] uppercase text-[var(--muted-foreground)]">02</span>
            <div class="h-px flex-1 bg-[var(--border)]"></div>
            <span class="text-sm font-semibold text-[var(--foreground)]">Website Services</span>
          </div>

          <div class="grid md:grid-cols-2 gap-3">
            {[
              { value: "Website Creation & Hosting", desc: "Custom websites and reliable hosting solutions" },
              { value: "Domain Management & Custom Emails", desc: "Professional email addresses and domain setup" },
              { value: "Social Media Setup & Branding", desc: "One-time setup and brand presence establishment" },
              { value: "Social Media Management", desc: "Ongoing content creation and posting" },
            ].map(({ value, desc }) => (
              <label class="group flex items-start gap-3 p-4 rounded-xl border border-[var(--border)] bg-[var(--background)] cursor-pointer transition-all duration-200 hover:border-[var(--primary)]/50 hover:bg-[var(--accent)] has-[:checked]:border-[var(--primary)] has-[:checked]:bg-[var(--accent)]">
                <input
                  type="checkbox"
                  name="services"
                  value={value}
                  class="mt-0.5 h-4 w-4 shrink-0 accent-[var(--primary)] rounded"
                />
                <div>
                  <span class="text-sm font-medium text-[var(--foreground)] block mb-0.5">{value}</span>
                  <p class="text-xs text-[var(--muted-foreground)] leading-relaxed">{desc}</p>
                </div>
              </label>
            ))}
          </div>
        </div>

        <!-- Content Creation -->
        <div>
          <div class="flex items-center gap-3 mb-6">
            <span class="text-xs font-mono tracking-[0.15em] uppercase text-[var(--muted-foreground)]">03</span>
            <div class="h-px flex-1 bg-[var(--border)]"></div>
            <span class="text-sm font-semibold text-[var(--foreground)]">Content Creation</span>
          </div>

          <div class="grid md:grid-cols-2 gap-3">
            {[
              { value: "Photo & Video Production", desc: "On-location photo and video content for your business" },
              { value: "Content Editing", desc: "Professional editing for photos and videos" },
              { value: "Professional Headshots", desc: "High-quality headshots for you and your team" },
            ].map(({ value, desc }) => (
              <label class="group flex items-start gap-3 p-4 rounded-xl border border-[var(--border)] bg-[var(--background)] cursor-pointer transition-all duration-200 hover:border-[var(--primary)]/50 hover:bg-[var(--accent)] has-[:checked]:border-[var(--primary)] has-[:checked]:bg-[var(--accent)]">
                <input
                  type="checkbox"
                  name="services"
                  value={value}
                  class="mt-0.5 h-4 w-4 shrink-0 accent-[var(--primary)] rounded"
                />
                <div>
                  <span class="text-sm font-medium text-[var(--foreground)] block mb-0.5">{value}</span>
                  <p class="text-xs text-[var(--muted-foreground)] leading-relaxed">{desc}</p>
                </div>
              </label>
            ))}
          </div>
        </div>

        <!-- Additional Details -->
        <div>
          <div class="flex items-center gap-3 mb-6">
            <span class="text-xs font-mono tracking-[0.15em] uppercase text-[var(--muted-foreground)]">04</span>
            <div class="h-px flex-1 bg-[var(--border)]"></div>
            <span class="text-sm font-semibold text-[var(--foreground)]">Additional Details</span>
          </div>

          <div>
            <label for="message" class="block text-xs font-mono tracking-wider uppercase text-[var(--muted-foreground)] mb-2">
              Tell us about your needs
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              placeholder="Tell us more about your project, timeline, budget, or any specific requirements..."
              class="w-full px-4 py-3 rounded-xl border border-[var(--border)] bg-[var(--input)] text-[var(--foreground)] placeholder:text-[var(--muted-foreground)]/40 focus:outline-none focus:border-[var(--ring)] focus:ring-2 focus:ring-[var(--ring)]/30 transition-all text-sm resize-none"
            ></textarea>
          </div>
        </div>

        <!-- Submit -->
        <div class="pt-2">
          <button
            type="submit"
            class="group w-full py-4 px-8 rounded-xl font-semibold text-sm tracking-wide transition-all duration-200 flex items-center justify-center gap-2"
            style="background: var(--primary); color: var(--primary-foreground);"
            onmouseover="this.style.opacity='0.85'"
            onmouseout="this.style.opacity='1'"
          >
            Submit Request
            <svg class="w-4 h-4 transition-transform duration-200 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </button>
          <p class="text-xs text-center text-[var(--muted-foreground)] mt-3 font-mono tracking-wide">
            We'll get back to you within 24 hours
          </p>
        </div>

      </form>
    </div>

  </div>
</section>
</Layout>